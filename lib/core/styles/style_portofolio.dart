import 'package:flutter/material.dart';

import '../constant/color_app.dart';

BoxDecoration kHeaaderStyle = BoxDecoration(
    gradient: LinearGradient(colors: [
      Colors.transparent,
      bgLight1
    ]),
    borderRadius: BorderRadius.circular(100)
);